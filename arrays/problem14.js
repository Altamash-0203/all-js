// print last three item from array

let cart=["watch","shoes","bag","mobile","googels","cooking oil","cap","food","cloths"]

let removes=cart.length-3  // subtract value always chnage as per hoe many itmes we print

for(i=removes;i<cart.length;i++)
{
   console.log(cart[i])
}

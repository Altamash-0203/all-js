// find average of marks

let marks=[56,32,53,35,53,53]
let sum =0

for(i=0;i<marks.length;i++)
    {
        sum=sum+marks[i]
       
    }
    console.log("total marks=",sum)
    let average=sum/marks.length

    console.log("avrage is =",average)